# Wizard Dungeon Explorer

Basic browser dungeon game with `W/A/S/D` movement.

## Run the game

```bash
npm run serve
```

Then open `http://localhost:8080`.

Sprites are loaded from `assets/sprites/manifest.json`.
